{
  "name": "clarify-search",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:ssr && node scripts/prerender.mjs",
    "build:client": "vite build",
    "build:ssr": "vite build --ssr ../src/entry-server.jsx --outDir ../dist-ssr",
    "build:preview": "PREVIEW_BUILD=1 vite build",
    "preview": "vite preview",
    "deploy": "node scripts/deploy-to-root.mjs"
  },
  "dependencies": {
    "framer-motion": "^11.11.17",
    "gsap": "^3.13.0",
    "lenis": "^1.1.18",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.4",
    "vite": "^8.1.5"
  },
  "browserslist": [
    ">0.3%",
    "last 2 versions",
    "Firefox ESR",
    "not dead"
  ]
}
